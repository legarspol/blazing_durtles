package com.smouldering_durtles.wk.ui.components

/**
 * Strings baked into the shared components themselves — content descriptions and the like, which
 * belong to the component rather than to whichever screen hosts it.
 *
 * Screen copy stays with its feature (`OnboardingStrings`, …); only text a component would have to
 * ask every caller for lives here.
 */
object ComponentStrings {
    const val back = "Back"
}
