package com.smouldering_durtles.wk

/**
 * Placeholder confirming the Kotlin toolchain is wired up in :app and
 * compiles alongside the existing Java sources. Delete once real Kotlin
 * code lands in the migration.
 */
object KotlinToolchainCheck {
    const val MESSAGE: String = "Kotlin toolchain is set up"
}
